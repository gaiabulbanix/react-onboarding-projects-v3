export default function FormInput({ label, placeholder, className = '', }) {
    return (
        <div>
            <div>

            </div>

            <input type="text" />
        </div>
    );
}